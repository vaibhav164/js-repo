/** iterable are thoes elements in JS to which we can use loops 
 * e.g., string or array */

/*** array like objects as we know objects are not iterable but array like objects are
 * those who hase length property and can be accessed using index e.g., string;
 */

let names = 'vaibhav';

// console.log(names.length);
// console.log(names[4], names['4'])

/*** as we can see in L10 and L11 we can access the length of names string and 
 * can access the string using index thus it can be called as array like objects
 */
